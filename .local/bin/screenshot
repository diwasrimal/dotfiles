#!/bin/bash

# Usage: screenshot    : Capture whole screen
#        screenshot -s : Select a part of screen to capture

filename="$(date '+%F-%H-%M').png"

maim $1 | tee ~/pics/Screenshots/$filename | xclip -selection clipboard -t image/png

notify-send -i ~/.config/icons/image.png "Image copied to clipboard"

