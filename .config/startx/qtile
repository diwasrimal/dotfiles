xset r rate 200 25 &
nitrogen --restore &
# nvim --headless &

# Map right Alt key as super
xmodmap ~/.Xmodmap

# MPD daemon start (if no other user instance exists)
#[ ! -s ~/.config/mpd/pid ] && mpd

# Dwm 
dunst &
picom &
qtile start 
